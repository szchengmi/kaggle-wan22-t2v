# ===== Cell 1: 安装 =====
# ⚠️ 把下面的 token 替换成你的 GitHub Personal Access Token
# 获取方式: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
# 只需要勾选 repo 权限即可
GITHUB_TOKEN = "PASTE_YOUR_TOKEN_HERE"

!pip install -q torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
!git clone -q https://{GITHUB_TOKEN}@github.com/comfyanonymous/ComfyUI.git
%cd ComfyUI
!pip install -q -r requirements.txt
!pip install -q xformers gguf accelerate Pillow
!git clone -q https://{GITHUB_TOKEN}@github.com/city96/ComfyUI-GGUF.git custom_nodes/ComfyUI-GGUF
!ls custom_nodes/
print("✅ 安装完成")
